
1) Make sure to add change the absolute paths of all images in SmartCity.fxml to make sure images are displayed properly
2) I've attached the SQL insertion queries aswell. I recommend to keep the name & titles to one word for easy searching
3) I don't think this project requires a class diagram or sequence diagrams as it contains only 4 classes and not much user/inter-class interaction to make a purposeful diagram
